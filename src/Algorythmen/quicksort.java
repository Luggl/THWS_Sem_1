/*
package Algorythmen;

public class quicksort {

    public int[] quicksort(int[] originalListe){

        if(originalListe.length == 1){
            return originalListe;
        }else{
            int[] links = new int[originalListe.length / 2];
            int[] rechts = new int[originalListe.length/2];
            for(int i = 0; i < originalListe.length; i++){
                if(i<originalListe.length/2){
                    links[i] = originalListe[i];

                }else{
                    rechts[i] = originalListe[i];
                }
                quicksort(links);
                quicksort(rechts);
            }
        }
        int[] out = new int[originalListe.length];
        for

        return
    }
}
*/
